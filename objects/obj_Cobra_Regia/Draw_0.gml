draw_self();

/*// Pega os limites da mask atual
var left   = bbox_left;
var right  = bbox_right;
var top    = bbox_top;
var bottom = bbox_bottom;

// Desenha o retângulo da colisão
draw_set_alpha(0.5);
draw_set_color(c_red);
draw_rectangle(left, top, right, bottom, false);
draw_set_alpha(1);

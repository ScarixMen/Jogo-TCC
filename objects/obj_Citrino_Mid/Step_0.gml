if (Check_Pause()) exit;

script_execute(state)

	/*var distancia = point_distance(x, y, obj_Player.x, obj_Player.y);
	var distancia_max = 1000;
	var distancia_min = 30;
	var fator_volume = 1.5 * clamp(1 - (distancia - distancia_min) / (distancia_max - distancia_min), 0, 100);
	
	if(distancia < distancia_max)audio_sound_gain(sfx_Arrow, fator_volume, 0);*/
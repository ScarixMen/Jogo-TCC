if (locked) {
    sprite_index = sprite_on;
    image_speed = 0;
} else if (activated) {
    sprite_index = sprite_on;
    image_speed = 0;
} else {
    sprite_index = sprite_idle;
    image_speed = 0;
}
